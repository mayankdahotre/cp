#include <bits/stdc++.h>
using namespace std;

#define int long long

bool val(int x, int y, int n){
    x--;
    y--;

    
}

void solve() {
    int n;
    cin>>n;

    int q;
    cin>>q;

    while(q--){
        string dir;
        cin>>dir;

        if(dir=='->'){
            int x, y;
            cin>>x>>y;
        }

        if(dir=="<-"){
            int d;
            cin>>d;
        }
    }

}

int32_t main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int T;
    cin >> T;
    while (T--) {
        solve();
    }
    return 0;
}