#include <bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int T;
    cin >> T;
    while (T--) {
        long long n, x;
        cin >> n >> x;
        cout<<"add "<<n-x<<endl;
        cout.flush();
    }

    return 0;
}
